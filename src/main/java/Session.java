public class Session {
    SQLConnector connection;
    // inserts to table
    // look for class in builtTables
    // if exists then do
    void save(Class<?> c, Object o) {
        //get object fields
        //insert into class table
    }

    // call to table
    void getAll(Class<?> c) {
       // select * from class==table name
    }

    // ?.where(or (new LessThanConstraint(), new EqualConstraint())
}
